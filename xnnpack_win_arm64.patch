--- a/BUILD.bazel
+++ b/BUILD.bazel
@@ -16228,6 +16228,11 @@
 )

 config_setting(
+    name = "windows_arm64",
+    values = {"cpu": "arm64_windows"},
+)
+
+config_setting(
     name = "windows_x86_64_clang",
     values = {
         "compiler": "clang-cl",
