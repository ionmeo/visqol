--- a/zutil.h
+++ b/zutil.h
@@ -144,7 +144,9 @@
 #  ifdef NO_FSEEKO
 #    define ZLIB_NO_FSEEKO
 #    define fseeko(f, o, w) fseek((f), (o), (w))
+#    if !defined(__APPLE__)
 #        define fdopen(fd,mode) NULL /* No fdopen() */
+#    endif
 #  endif
 #endif
